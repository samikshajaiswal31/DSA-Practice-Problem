/*  Delete an element from an Array
    I/P : arr[] = {3,8,7,4,1}  element delete : - 4   
    O/P: 3 8 7 1 */
    
    
import java.util.Arrays;
class HelloWorld 
{
    public static void main(String[] args) 
    {
        Integer arr[] = {3,8,7,4,1}, x = 4;
        int index = Arrays.asList(arr).indexOf(x);
        
        for(int i = x-1 ; i < arr.length-1 ; i++)   
            arr[i] = arr[i+1];
           
        for(int i = 0 ; i < arr.length-1 ; i++)   
           System.out.print(arr[i]+" "); 
    }
}

-------------------------------------------------------------------------

/*  Delete an element from an Array at specified position
    I/P : arr[] = {3,8,7,4,1} , index = 2    
    O/P: 3 7 4 1 */
    
import java.util.Arrays;
class HelloWorld 
{
    public static void main(String[] args) 
    {
        Integer arr[] = {3,8,7,4,1}, x = 2;
        //int index = Arrays.asList(arr).indexOf(x);
        //if()
        for(int i = x-1 ; i < arr.length-1 ; i++)   
            arr[i] = arr[i+1];
           
        for(int i = 0 ; i < arr.length-1 ; i++)   
           System.out.print(arr[i]+" "); 
    }
}
